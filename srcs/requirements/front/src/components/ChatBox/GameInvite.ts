import io from 'socket.io-client'; // Import the socket.io client
import { Socket } from 'socket.io-client'; // Import the socket.io client

export function sendPongGameInvite(userID: number, guestID: number) {
    
}